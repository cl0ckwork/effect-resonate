services:
  postgres:
    build:
      context: ./docker
    command:
      - postgres
      - -c
      - shared_preload_libraries=pg_cron
      - -c
      - cron.database_name=postgres
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres_test_password
    ports:
      - "127.0.0.1::5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d postgres"]
      interval: 2s
      timeout: 2s
      retries: 30

  integresql:
    image: ghcr.io/allaboutapps/integresql@sha256:66b7433399f1907bad9e4b7cc7bd528ed4ebfccddecb8fd65da4af4d06a68c5a
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      INTEGRESQL_PGHOST: postgres
      INTEGRESQL_PGUSER: postgres
      INTEGRESQL_PGPASSWORD: postgres_test_password
      INTEGRESQL_PGDATABASE: postgres
      INTEGRESQL_PORT: "5000"
    ports:
      - "127.0.0.1::5000"
